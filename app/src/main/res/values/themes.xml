<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.LabSheets" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.LabSheets" parent="Base.Theme.LabSheets" />

    <style name="TextViewStyle">
        <item name="android:textColor">@color/primaryTextColor</item>
        <item name="android:textSize">@dimen/primary_text_size</item>
        <item name="android:layout_marginStart">@dimen/app_horizontal_margin</item>
        <item name="android:layout_marginEnd">@dimen/app_horizontal_margin</item>
    </style>
</resources>